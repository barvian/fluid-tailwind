import Link from '/components/Link.astro'
export const components = { a: Link }

A [fluid `clamp()`](https://developer.mozilla.org/en-US/docs/Web/CSS/clamp) plugin for Tailwind that works with every utility.
