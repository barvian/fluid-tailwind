import Link from '/components/Link.astro'
export const components = { a: Link }

A [fluid `clamp()`](https://www.smashingmagazine.com/2022/01/modern-fluid-typography-css-clamp/) plugin for Tailwind that works with every utility.
